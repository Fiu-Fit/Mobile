export type inputProps = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  role: string;
};

export type errorInputProps = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
